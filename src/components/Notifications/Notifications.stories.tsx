/* eslint-disable */
import { storiesOf } from '@storybook/react';
import Notifications from './index';

storiesOf('Notifications', module).add('default', () => <Notifications isOpen onRequestClose={() => {}} />);
