/* eslint-disable */
import { storiesOf } from '@storybook/react';
import TransactionSlippage from './index';

// storiesOf('TransactionSlippage', module).add('default', () => <TransactionSlippage />);
